import React, { FC } from "react";
import { Text } from "react-native";

export interface MojeProps {}

export const Moje_Ogloszenia: FC<MojeProps> = ({}) => {
  return <Text>Moje Ogłoszenia</Text>;
};
