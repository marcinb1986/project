import React, { FC } from "react";
import { Text } from "react-native";
import { Wrapper } from "../../../wrapper/wrapper";

export interface KontaktProps {}

export const Kontakt: FC<KontaktProps> = ({}) => {
  return <Wrapper>Kontakt</Wrapper>;
};
