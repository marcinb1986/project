import React, { FC } from "react";
import { Text } from "react-native";
import { Wrapper } from "../../../wrapper/wrapper";

export interface MojeOgloszeniaProps {}

export const MojeOgloszenia: FC<MojeOgloszeniaProps> = ({}) => {
  return <Wrapper>Moje Ogloszenia</Wrapper>;
};
