import React, { FC } from "react";
import { Text } from "react-native";
import { Wrapper } from "../../../wrapper/wrapper";

export interface PremiumProps {}

export const Premium: FC<PremiumProps> = ({}) => {
  return <Wrapper>Premium</Wrapper>;
};
